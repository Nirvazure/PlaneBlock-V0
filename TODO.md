# PlaneBlock TODO

## 第一阶段（已完成）✅

### 核心功能
- [x] 用户登录/注册（CloudBase 手机号验证码）
- [x] 在线对战（房间创建/加入，实时同步）
- [x] 本地同屏对战
- [x] 战绩持久化（个人主页展示胜/负/胜率）
- [x] 好友系统（添加好友、好友请求、邀请对战）

### 实时同步方案演进
- [x] ~~CloudBase watch~~ → 在中国大陆不稳定，频繁断开（`SYS_ERR`）
- [x] ~~智能轮询~~ → 已移除，改为纯 WebSocket 全实时
- [x] **阿里云 WebSocket** → 第四阶段已实现，全实时推送

### 技术决策记录
| 问题 | 尝试方案 | 结果 |
|------|---------|------|
| CloudBase watch 不稳定 | 增强 auth 检查、token 刷新 | ❌ SDK 限制，无法修复 |
| 替代方案：腾讯云 API 网关 WebSocket | - | ❌ 产品已停售 |
| 替代方案：Pusher | - | ❌ 中国大陆不稳定 |
| 替代方案：轻量服务器 | 阿里云轻量 + WebSocket | ✅ 已采纳 |
| **第四阶段：全实时** | 双云架构，WebSocket 替代轮询 | ✅ 已实现 |

## 待办

### 第二阶段（功能扩展）

| 序号 | 内容 | 优先级 | 说明 |
|------|------|--------|------|
| 1 | 随机匹配陌生人 | 低 | 需要设计匹配队列 |
| 2 | 战绩排行榜 | 低 | 全服排名 Top 100 |
| 3 | 游戏回放 | 低 | 保存对战记录，支持回放 |

### 第三阶段（性能优化）

| 序号 | 内容 | 优先级 | 说明 |
|------|------|--------|------|
| 1 | 阿里云 WebSocket 全实时 | 高 | ✅ 已实现：ws-server + 前端 hooks，纯 WebSocket 无轮询 |
| 2 | PWA 支持 | 低 | 支持安装到桌面 |
| 3 | 移动端优化 | 中 | 触摸操作优化 |

### 第四阶段架构（双云·全实时）

数据流与职责划分：

```
┌─────────────┐     HTTP API      ┌──────────────────────┐
│   Vercel    │ ◀───────────────▶ │ 腾讯云 CloudBase      │
│ Next.js API │   (rooms/friends  │ (Auth + 数据库)       │
└──────┬──────┘   invites...)     └──────────────────────┘
       │                                    ▲
       │ 调用 GET/PATCH rooms 读写房间       │
       │ （可选：WebSocket 服务通过 HTTP     │
       │  代理调用，或直接连 CloudBase）     │
       ▼                                    │
┌──────────────────────┐                    │
│ 阿里云轻量服务器      │ ──────────────────┘
│ WebSocket 服务        │   (若需直接读 DB)
│ - 房间状态广播        │
│ - 邀请/好友推送       │
└──────────▲───────────┘
           │
     WebSocket 长连接
           │
┌──────────┴───────────┐
│ 前端 (浏览器)         │
└──────────────────────┘
```

**职责说明：**

| 组件 | 职责 |
|------|------|
| 腾讯云 CloudBase | Auth（手机号登录）、数据库（房间/好友/邀请/战绩） |
| Vercel Next.js API | 业务 API（创建房间、加入、更新状态、好友、邀请） |
| 阿里云轻量服务器 | WebSocket 实时推送（房间状态、邀请、好友请求） |
| 前端 | HTTP 调用 Vercel API + WebSocket 连接阿里云接收推送 |

---

## 归档

### 第一阶段总结（2026-03-17）

**目标：** 实现可用的在线对战 demo

**成果：**
- ✅ 完整的游戏流程（登录 → 创建/加入房间 → 对战 → 结算）
- ✅ 好友系统（添加、邀请、管理）
- ✅ WebSocket 全实时同步（无轮询）
- ✅ 中国大陆可用（无需翻墙、无海外依赖）
- ✅ 零运维成本（Vercel Serverless + 阿里云轻量 WS）

**技术栈：**
- Next.js 14 + CloudBase + Vercel + 阿里云 ws-server
- WebSocket 推送 + 同步中 loading + 操作锁

**近期优化（2026-03-17）：**
- 修复 invites 接口因 effect 依赖导致频繁请求
- 同步中禁用放置/攻击操作，防止重复点击
- 对方回合 / 同步中 / 轮到你 状态区分展示
- 首页与对战入口合并：规则+对战入口同屏展示，100vh 布局，页脚固定底部

